{
  "name": "ai-self-funding-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "projects/ai-workflow-library"
  ],
  "scripts": {
    "validate:dashboard": "node scripts/validate-dashboard",
    "dev": "npm run dev --workspace=ai-workflow-library",
    "build": "npm run build --workspace=ai-workflow-library",
    "test": "npm run test --workspace=ai-workflow-library",
    "check:ai-workflow-library": "npm run test --workspace=ai-workflow-library"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
