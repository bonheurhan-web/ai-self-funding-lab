# Shared Components

Framework-specific components proven useful in at least two projects belong here.

Each component must document:

- Supported framework and version
- Public API
- Accessibility behavior
- Styling dependencies
- Tests
- Known consumers

Do not add speculative components for possible future use.
